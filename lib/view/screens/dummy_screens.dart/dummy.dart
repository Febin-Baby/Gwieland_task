import 'package:flutter/material.dart';

class PageEshop extends StatelessWidget {
  const PageEshop({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('E shop'),
      ),
    );
  }
}

class Sun extends StatelessWidget {
  const Sun({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Sun'),
      ),
    );
  }
}

class LauchPads extends StatelessWidget {
  const LauchPads({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('LauchPads'),
      ),
    );
  }
}

class Wallet extends StatelessWidget {
  const Wallet({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Wallet'),
      ),
    );
  }
}
